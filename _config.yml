# ========================================================================
# Blog personal settings
# ========================================================================

# Themes are encouraged to use these universal variables 
# so be sure to set them if your theme uses them. 
title: Maxint Lin
tagline: On My Way

username: maxint
full_name: Naiyan Lin
author: Naiyan Lin (maxint)
email: '&#108;&#110;&#121;&#099;&#104;&#105;&#110;&#097;&#064;&#103;&#109;&#097;&#105;&#108;&#046;&#099;&#111;&#109;'

production_url : http://maxint.github.io
theme : default


# ========================================================================
# All jekyll-Bootstrap specific configuration
# ========================================================================
JB:
  # All links will be namespaced by BASE_PATH if defined
  BASE_PATH : false

  # By default, the ASSET_PATH is automatically defined relative to BASE_PATH
  # plus the enabled theme name.
  # ex: [BASE_PATH]/assets/themes/[THEME-NAME]
  #
  # Override this by defining an absolute path to assets here.
  # ex: 
  #   http://s3.amazonaws.com/yoursite/themes/watermelon/assets
  ASSET_PATH : false

  # Comments helper
  # Set 'provider' to the comment provider you want to use.
  # Set 'provider' to false to turn commneting off globally.
  comments :
    provider : uyan
    disqus :
      short_name : mgithub
    uyan :
      id : 1948160

  # Analytics helper
  analytics :
    provider : google
    google :
      tracking_id : 'UA-XXX-XX'

  # Sharing helper
  # Sharing is for things like tweet, plusone, like, reddit buttons etc.
  sharing:
    provider: false

  # Setting any helper's provider to 'custom' will bypass the helper code
  # and include your custom code. Your custom file must be defined at:
  #   ./_includes/custom/[HELPER]
  # where [HELPER] is the name of the helper you are overriding


# ========================================================================
# Jekyell system settings
# ========================================================================

# Global configuration
source : .
destination : ./_site
plugins : ./_plugins
layouts : ./_layouts
timezone : Aisa/Beijing

#relative_permalinks : true
# This is the default format. 
# For more see: https://github.com/mojombo/jekyll/wiki/Permalinks
#permalink : date
#permalink: /:categories/:year/:month/:day/:title 
#paginate_path : 'page:num'

safe: false # for debug
markdown : redcarpet # rdiscount
highlighter: pygments # The allowed values are 'rouge', 'pygments' or null
#markdown_ext : markdown,mkd,mkdn,md
#textile_ext : textile

# Serve settings
#port: 4000
#host: HOSTNAME
#baseurl: URL


# github pages features
gems:
  - jekyll-sitemap # generate sitemap automatically
  - jekyll-redirect-from # enable recirects, usage: --- redirect_from: "/foo" ---
  - jekyll-mentions # enable @mention
  - jemoji # enable emoji

github:
  username: maxint/maxint.github.io

defaults:
  -
    scope:
      type: "posts"
    values:
      layout: "post"
      author: "maxint"
    

# vim: et ts=2 sts=2 sw=2
